# Corpus index

See [README.md](./README.md) for entrypoints and policy.

**Manifest file count:** see `MANIFEST.json` → `file_count` (expect 325 content files from va snapshot).

**Agent map:** `AGENT_DOC_MAP.json` maps each `pack_id` → related corpus paths.
