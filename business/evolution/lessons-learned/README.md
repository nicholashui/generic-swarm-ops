# Lessons learned

Runtime reflections write JSON artifacts here (`{run_id}.json`) when
`POST /api/v1/improvement/reflect/{run_id}` succeeds.

Also stored in runtime collection `improvement_lessons` (Postgres-backed).
