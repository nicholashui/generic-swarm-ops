import { designTokens } from "./tokens";

export const appTheme = {
  brand: "Generic Swarm Ops",
  tokens: designTokens,
  typography: {
    body: "IBM Plex Sans",
    mono: "IBM Plex Mono",
  },
} as const;
