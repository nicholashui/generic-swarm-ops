# Layout Decisions

- App shell uses a persistent left sidebar and sticky header.
- Dashboard prioritizes metrics, approvals, knowledge health, and recent activity.
- Workflow run detail uses an operations split view for timeline plus logs.
- Settings remain panel-driven and administratively dense.
- Mobile navigation collapses into a drawer trigger in the header.
